-- SQLite
-- SELECT DISTINCT tracks.id, tracks.name, albums.id
-- FROM albums
-- JOIN tracks ON albums.id = tracks.album_id
-- JOIN genres ON genres.id = tracks.genre_id
-- WHERE albums.artist_id = 230;

SELECT * FROM tracks;
